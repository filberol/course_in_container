# Terraform для Лаб 4. Опиши инфраструктуру декларативно.
# Требования (см. задания): `terraform validate` проходит; `apply` идемпотентен
# (повторный plan — без изменений); ресурсы реально поднимаются.
# Провайдер — kind/docker (поднять кластер и развернуть example-voting-app) или
# другой по варианту.
#
# TODO:
# terraform {
#   required_providers { ... }
# }
# provider "..." { ... }
# resource "..." "..." { ... }
